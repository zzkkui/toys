import Clayout from "../../../layout/index.tsx";
import { Button } from "antd";

export const meta = {
  title: "Md 文件",
};

# My MDX page

This is a list in markdown:

- One
- Two
- Three

Checkout my React component:

<Button>123</Button>

export default ({ children }) => <Clayout {...meta}>{children}</Clayout>;
